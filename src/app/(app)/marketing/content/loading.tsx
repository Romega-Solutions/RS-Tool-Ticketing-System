import { PageLoadingSkeleton } from "@/components/page-loading-skeleton";

export default function Loading() {
  return (
    <PageLoadingSkeleton
      title="Marketing / Content"
      description="Loading content tooling."
      variant="form"
    />
  );
}
